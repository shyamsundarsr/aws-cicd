name: Github actions - AWS CICD

on:
  push:
    branches: [main]
    
permissions:
  id-token: write
  contents: read

jobs:
  terraform:
    uses: ./.github/workflows/terraform.yml
  deploy:
    uses: ./.github/workflows/aws-ecs-deploy.yml